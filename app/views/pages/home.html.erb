<div class="header-home">
  <div class="header-logo">
    <%= image_tag "BookBuddy_logo.svg", alt: "Logo BookBuddy", class: "book"%>
    <%= image_tag "BookBuddy.svg", alt: "BookBuddy", class:"text" %>
  </div>
  <div class="header-user">
    <i class="fa-solid fa-user"></i>
  </div>
</div>

<div class="home-card">
  <h4>Bienvenue  <strong><%= current_user.nickname %></strong> !</h4>
  Il y a <strong><%= current_user.books.count %></strong> livres dans ta bibliothèque :
    <strong><%= current_user.books.read_books.count %></strong> sont
  lus et
    <strong><%= Book.all.favorite_books(true, current_user.id).count %></strong> sont favoris.
    Tu as <strong class='green-text'><%= current_user.books.where(book_type: 'Roman').count %> romans</strong>,
    <strong class='blue-text'><%= current_user.books.where(book_type: 'Manga').count %> manga</strong>
  , <strong class='red-text'><%= current_user.books.where(book_type: 'BD').count %> bande-dessinées</strong> et
    <strong class='purple-text'><%= current_user.books.where(book_type: 'Poesie').count %> recueils de
  poésie</strong>.

</div>

<div class="last-books">
  <h2><i class="fa-solid fa-book-bookmark home-icon-book"></i> <span style="text-decoration: underline;
                        text-decoration-style: solid;
                        text-decoration-color: #89E219;
                        text-decoration-thickness: 0.4rem;
                        text-underline-offset: 0.1rem;">Derniers Ajouts</span></h2>
  <div class="last-books-cards">
    <% current_user.books.reverse.each do |book| %>
      <%= link_to book_path(book) do %>
        <%= render 'partials/books/book_card_portrait', book: book %>
      <% end %>
    <% end %>
  </div>
</div>

<div class="last-books">
  <h2><i class="fa-solid fa-heart home-icon-heart"></i></i> <span style="text-decoration: underline;
                        text-decoration-style: solid;
                        text-decoration-color: #FF6359;
                        text-decoration-thickness: 0.4rem;
                        text-underline-offset: 0.1rem;">Livres Favoris</span></h2>
  <div class="last-books-cards">
    <% current_user.books.favorite_books('true', current_user).each do |book| %>
      <%= link_to book_path(book) do %>
        <%= render 'partials/books/book_card_portrait', book: book %>
      <% end %>
    <% end %>
  </div>
</div>
